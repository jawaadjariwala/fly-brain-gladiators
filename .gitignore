# Connectome data: ~1.1 GB raw, ~205 MB matrix. NEVER commit.
# Rooted: an unanchored `data/` also matches web/data/, which ships.
/data/
*.npz
*.npy
*.h5
*.hdf5
*.parquet
*.feather
connectome/
malecns/

# Rendered output
renders/
clips/
*.mp4
*.mov
*.gif
!docs/**/*.gif

# Match results: keep summaries, not raw spike dumps
runs/**/spikes/
runs/**/raw/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env
*.egg-info/
.ipynb_checkpoints/
.pytest_cache/
.ruff_cache/

# macOS
.DS_Store

# built artifacts
*.partial

# The match library IS the site's content, it ships. Rebuild it with
# scripts/export_matches.py; it is regenerable, but the published player
# needs it present.
# Two matches from an early test run, written before pair slugs were ordered
# alphabetically. Superseded by cassius__octavian__*, not in index.json, and
# left on disk rather than deleted.
web/data/octavian__cassius__s*.fbg
